name: Rad Package
type: rad/package

parameters:
  type: object
  properties:
    name:
      type: string
      title: Cluster Name
  required:
    - name

inputs:
  type: object
  properties:
    vpc:
      type: object
      properties:
        id:
          type: string

outputs:
  type: object
  properties:
    cluster:
      type: object
      properties:
        id:
          type: string
        endpoint:
          type: string
