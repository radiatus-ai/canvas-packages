name: Rad App
type: rad/app

parameters:
  type: object
  properties:
    name:
      type: string
      title: App Name
  required:
    - name

inputs:
  type: object
  properties:
    cluster:
      type: object
      properties:
        id:
          type: string
    instance:
      type: object
      properties:
        id:
          type: string

outputs:
  type: object
  properties:
    cluster:
      type: object
      properties:
        id:
          type: string
        endpoint:
          type: string
