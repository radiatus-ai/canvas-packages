name: GCP VPC
type: gcp/vpc

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name
      minLength: 4
      maxLength: 20
  required:
    - name

inputs:
  type: object
  properties: {}

outputs:
  type: object
  properties:
    network:
      type: object
      properties:
        id:
          type: string
