name: PubSub
type: gcp/pubsub

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name
      minLength: 4
      maxLength: 20
  required:
    - name

inputs:
  type: object
  properties: {}

outputs:
  type: object
  properties:
    topic:
      type: object
      properties:
        id:
          type: string
